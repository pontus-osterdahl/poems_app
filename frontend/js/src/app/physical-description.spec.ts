import { PhysicalDescription } from './physical-description';

describe('PhysicalDescription', () => {
  it('should create an instance', () => {
    expect(new PhysicalDescription()).toBeTruthy();
  });
});
