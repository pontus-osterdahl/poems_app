import { ManuscriptDescription } from './manuscript-description';

describe('ManuscriptDescription', () => {
  it('should create an instance', () => {
    expect(new ManuscriptDescription()).toBeTruthy();
  });
});
