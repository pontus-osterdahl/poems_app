import { BibliographySource } from './bibliography-source';

describe('BibliographySource', () => {
  it('should create an instance', () => {
    expect(new BibliographySource()).toBeTruthy();
  });
});
